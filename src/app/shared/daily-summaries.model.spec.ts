import { DailySummariesModel } from './daily-summaries.model';

describe('DailySummariesModel', () => {
  it('should create an instance', () => {
    expect(new DailySummariesModel()).toBeTruthy();
  });
});
